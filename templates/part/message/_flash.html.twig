{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }} custom-alert" role="alert">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}
